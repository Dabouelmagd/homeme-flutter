name: homeme
description: HomeMe — نظام إدارة الكمبوندات السكنية
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.4
  webview_flutter_android: ^3.12.1
  webview_flutter_wkwebview: ^3.9.5
  connectivity_plus: ^5.0.2
  package_info_plus: ^4.2.0
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
